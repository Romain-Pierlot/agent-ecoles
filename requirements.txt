# LLM et orchestration
openai
langchain
langchain-openai
langchain-community
langgraph

# Base de données
# sqlite3 est inclus dans Python — pas besoin de l'installer
sqlglot  # validation du SQL généré par le LLM avant exécution (guardrails/sql_safety.py)

# Vectorstore RAG
chromadb

# Chargement variables d'environnement
python-dotenv

# Data processing
pandas

# Interface utilisateur
streamlit

# API backend (Next.js appelle l'agent via cette couche HTTP)
fastapi
uvicorn
psycopg[binary]  # écriture des logs de conversation vers Supabase (Postgres)

# Monitoring
langsmith

# Re-ranking
sentence-transformers
